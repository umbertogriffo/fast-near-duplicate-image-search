#!/usr/bin/env bash
# We can also install the package with a symlink, so that changes to the source files will be immediately available
# to other users of the package on our system:
pip3 install -e .